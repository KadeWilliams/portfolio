package templates

import (
    "time"
)

templ SystemInfo(ipAddress string) {
    <div class="mono">
        <p>SERVER TIME: { time.Now().Format(time.RFC1123) }</p>
        <p>CONNECTION: { ipAddress }</p>
    </div>
}